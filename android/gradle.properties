org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler cache can't relativize paths across two
# different Windows drive letters (project on D:, Pub/Gradle caches on C:),
# which crashes plugin compilation. Incremental compilation is disabled to
# avoid that; full compiles are a little slower but reliable.
kotlin.incremental=false
